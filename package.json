{
  "name": "perch",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "version": "1.0.0",
  "main": "index.js",
  "private": "true",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "db:reset": "prisma migrate reset",
    "build": "pnpm -r build",
    "build:shared": "pnpm --filter @perch/shared build",
    "dev:api": "pnpm build:shared && pnpm --filter @perch/api dev:server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "prisma": "7.10.0"
  }
}
