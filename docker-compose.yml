# NOTE: PostgreSQL is now hosted on Neon (neon.tech)
# Connection strings are in .env file
# Keeping this commented for reference only

# services:
#   postgres:
#     image: postgres:16
#     environment:
#       POSTGRES_USER: postgres
#       POSTGRES_PASSWORD: postgres
#       POSTGRES_DB: perch
#       POSTGRES_HOST_AUTH_METHOD: trust
#     ports:
#       - "5432:5432"
#     volumes:
#       - pgdata:/var/lib/postgresql/data

services:
  redis:
    image: redis:7
    ports:
      - "6379:6379"

# volumes:
#   pgdata: